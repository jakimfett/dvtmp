diff --git a/Makefile b/Makefile
index e0284b8..ee2feec 100644
--- a/Makefile
+++ b/Makefile
@@ -43,13 +43,11 @@ dist: clean
 	@rm -rf dvtmp-${VERSION}
 
 install: dvtmp
-	@echo stripping executable
-	@${STRIP} dvtmp
 	@echo installing executable file to ${DESTDIR}${PREFIX}/bin
 	@mkdir -p ${DESTDIR}${PREFIX}/bin
-	@cp -f dvtmp ${DESTDIR}${PREFIX}/bin
+	@cp -pf dvtmp ${DESTDIR}${PREFIX}/bin
 	@chmod 755 ${DESTDIR}${PREFIX}/bin/dvtmp
-	@cp -f dvtmp-status ${DESTDIR}${PREFIX}/bin
+	@cp -pf dvtmp-status ${DESTDIR}${PREFIX}/bin
 	@chmod 755 ${DESTDIR}${PREFIX}/bin/dvtmp-status
 	@echo installing manual page to ${DESTDIR}${MANPREFIX}/man1
 	@mkdir -p ${DESTDIR}${MANPREFIX}/man1
diff --git a/config.mk b/config.mk
index 273aa45..e4a2e85 100644
--- a/config.mk
+++ b/config.mk
@@ -12,8 +12,8 @@ TERMINFO := ${DESTDIR}${PREFIX}/share/terminfo
 INCS = -I.
 LIBS = -lc -lutil -lncursesw
 CPPFLAGS = -D_POSIX_C_SOURCE=200809L -D_XOPEN_SOURCE=700 -D_XOPEN_SOURCE_EXTENDED
-CFLAGS += -std=c99 ${INCS} -DVERSION=\"${VERSION}\" -DNDEBUG ${CPPFLAGS}
-LDFLAGS += ${LIBS}
+CFLAGS += -std=c99 ${INCS} -DVERSION=\"${VERSION}\" -DNDEBUG ${CPPFLAGS} ${RPM_OPT_FLAGS}
+LDFLAGS += ${LIBS} -znow
 
 DEBUG_CFLAGS = ${CFLAGS} -UNDEBUG -O0 -g -ggdb -Wall -Wextra -Wno-unused-parameter
 
